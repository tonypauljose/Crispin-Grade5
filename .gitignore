# ── WAT scratch & secrets ──
.tmp/
.env
credentials.json
token.json

# ── Python tooling ──
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/

# ── OS / Editor ──
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# ── Build / dependency ──
node_modules/
dist/
build/

# ── Logs ──
*.log
npm-debug.log*

# ── Local media (kept off the repo — large files) ──
Crispin Photos and Videos/

# ── Scanned school material (kept off the repo) ──
# This repo is published to crispintony.com via GitHub Pages, so scans of
# Crispin's notebooks — his name, his handwriting, his teacher's marking —
# must not be committed. They are the SOURCE for the apps built from them;
# keep them locally and in a private backup instead.
Half Yearly Portion/
Maths - Periodic Exam Reference/
Periodic Test - 1/
